import { starReview } from 'javascript-codereview-4/helpers/star-review';
import { module, test } from 'qunit';

module('Unit | Helper | star review');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = starReview([42]);
  assert.ok(result);
});
